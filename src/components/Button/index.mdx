---
name: Button
menu: Components
---

import { Playground, Props } from 'docz';
import Button from './';

# Button

Buttons make common actions more obvious and help users more easily perform them. Buttons use labels and sometimes icons to communicate the action that will occur when the user touches them.

## Variant

<Playground>
  <Button>Default</Button>
  <Button variant="primary">Primary</Button>
  <Button variant="dashed">Dashed</Button>
  <Button variant="link">Link</Button>
</Playground>

## Indent

<Playground>
  <Button variant="primary" indent="success">Primary</Button>
  <Button variant="dashed" indent="success">Dashed</Button>
  <Button variant="link" indent="success">Link</Button>
  <Button variant="primary" indent="danger">Primary</Button>
  <Button variant="dashed" indent="danger">Dashed</Button>
  <Button variant="link" indent="danger">Link</Button>
  <Button variant="primary" indent="warning">Primary</Button>
  <Button variant="dashed" indent="warning">Dashed</Button>
  <Button variant="link" indent="warning">Link</Button>
</Playground>

## Different sizes

<Playground>
  <Button size="small">Small</Button>
  <Button size="normal">Normal</Button>
  <Button size="large">Large</Button>
</Playground>

## Disabled

<Playground>
  <Button disabled>Disabled</Button>
</Playground>

## Loading

<Playground>
  <Button loading>Loading</Button>
</Playground>


## Properties

<Props of={Button} />
