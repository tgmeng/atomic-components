---
name: Button
menu: Components
---

import { Playground, Props } from 'docz';
import { ButtonGroup } from './ButtonGroup';
import { Button } from './';

# Button

## 不同变体

<Playground>
  <Button>Default</Button>
  <Button variant="primary">Primary</Button>
  <Button variant="dashed">Dashed</Button>
  <Button variant="link">Link</Button>
</Playground>

## 不同意图

<Playground>
  <Button variant="primary" intent="success">
    Primary
  </Button>
  <Button variant="dashed" intent="success">
    Dashed
  </Button>
  <Button variant="link" intent="success">
    Link
  </Button>
  <Button variant="primary" intent="danger">
    Primary
  </Button>
  <Button variant="dashed" intent="danger">
    Dashed
  </Button>
  <Button variant="link" intent="danger">
    Link
  </Button>
  <Button variant="primary" intent="warning">
    Primary
  </Button>
  <Button variant="dashed" intent="warning">
    Dashed
  </Button>
  <Button variant="link" intent="warning">
    Link
  </Button>
</Playground>

## 不同大小

<Playground>
  <Button size="small">Small</Button>
  <Button size="normal">Normal</Button>
  <Button size="large">Large</Button>
</Playground>

## 禁用

<Playground>
  <Button disabled>Disabled</Button>
  <Button variant="primary" disabled>Disabled primary</Button>
  <Button variant="dashed" disabled>Disabled dashed</Button>
  <Button variant="link" disabled>Disabled link</Button>
</Playground>

## 加载

<Playground>
  <Button loading>Loading</Button>
</Playground>

## ButtonGroup

<Playground>
  <ButtonGroup>
    <Button key="1">测试</Button>
    <Button key="2">...</Button>
  </ButtonGroup>
</Playground>

## 属性

<Props of={Button} />
