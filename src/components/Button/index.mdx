---
name: Button
menu: Components
---

import { Playground, Props } from 'docz';
import Button from './';

# Button

## 不同变体

<Playground>
  <Button>Default</Button>
  <Button variant="primary">Primary</Button>
  <Button variant="dashed">Dashed</Button>
  <Button variant="link">Link</Button>
</Playground>

## 不同意图

<Playground>
  <Button variant="primary" indent="success">
    Primary
  </Button>
  <Button variant="dashed" indent="success">
    Dashed
  </Button>
  <Button variant="link" indent="success">
    Link
  </Button>
  <Button variant="primary" indent="danger">
    Primary
  </Button>
  <Button variant="dashed" indent="danger">
    Dashed
  </Button>
  <Button variant="link" indent="danger">
    Link
  </Button>
  <Button variant="primary" indent="warning">
    Primary
  </Button>
  <Button variant="dashed" indent="warning">
    Dashed
  </Button>
  <Button variant="link" indent="warning">
    Link
  </Button>
</Playground>

## 不同大小

<Playground>
  <Button size="small">Small</Button>
  <Button size="normal">Normal</Button>
  <Button size="large">Large</Button>
</Playground>

## 禁用

<Playground>
  <Button disabled>Disabled</Button>
</Playground>

## 加载

<Playground>
  <Button loading>Loading</Button>
</Playground>

## 属性

<Props of={Button} />
