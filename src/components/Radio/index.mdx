---
name: Radio
menu: Components
---

import { Playground, Props } from 'docz';
import Radio, { RadioButton } from './';

# Radio

## 基本用法

<Playground>
  <Radio>Radio</Radio>
</Playground>

## 禁止状态

<Playground>
  <Radio disabled>Radio</Radio>
  <Radio checked disabled>
    Radio
  </Radio>
</Playground>

## Radio.Group

<Playground>
  {() => {
    const [value, setValue] = React.useState(1);
    return (
      <Radio.Group
        value={value}
        onChange={(value) => {
          setValue(value);
        }}
      >
        <Radio value={1}>选项一</Radio>
        <Radio value={2}>选项二</Radio>
        <Radio value={3} disabled>
          选项三
        </Radio>
      </Radio.Group>
    );
  }}
</Playground>

## RadioButton

### 基础用法

<Playground>
  {() => {
    const [value, setValue] = React.useState(1);
    return (
      <RadioButton.Group
        value={value}
        onChange={(value) => {
          setValue(value);
        }}
      >
        <RadioButton value={1}>选项一</RadioButton>
        <RadioButton value={2}>选项二</RadioButton>
        <RadioButton value={3} disabled>
          选项三
        </RadioButton>
      </RadioButton.Group>
    );
  }}
</Playground>

### 实底

<Playground>
  {() => {
    const [value, setValue] = React.useState(1);
    return (
      <RadioButton.Group
        variant="solid"
        value={value}
        onChange={(value) => {
          setValue(value);
        }}
      >
        <RadioButton value={1}>选项一</RadioButton>
        <RadioButton value={2}>选项二</RadioButton>
        <RadioButton value={3} disabled>
          选项三
        </RadioButton>
      </RadioButton.Group>
    );
  }}
</Playground>

### 不同大小

<Playground>
  {() => {
    const [value, setValue] = React.useState(1);
    return (
      <>
        <RadioButton.Group size="large">
          <RadioButton value={1}>选项一</RadioButton>
          <RadioButton value={2}>选项二</RadioButton>
          <RadioButton value={3}>选项三</RadioButton>
        </RadioButton.Group>
        <RadioButton.Group size="normal">
          <RadioButton value={1}>选项一</RadioButton>
          <RadioButton value={2}>选项二</RadioButton>
          <RadioButton value={3}>选项三</RadioButton>
        </RadioButton.Group>
        <RadioButton.Group size="small">
          <RadioButton value={1}>选项一</RadioButton>
          <RadioButton value={2}>选项二</RadioButton>
          <RadioButton value={3}>选项三</RadioButton>
        </RadioButton.Group>
      </>
    );
  }}
</Playground>

## Radio 属性

<Props of={Radio} />

## Radio.Group 属性

<Props of={Radio.Group} />

## RadioButton.Group 属性

<Props of={RadioButton.Group} />
