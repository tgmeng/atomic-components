---
name: Modal
menu: Components
---

import { Playground, Props } from 'docz';
import Button from '../Button';
import Modal from './index.tsx';

# Modal

## Basic
<Playground>
  {() => {
    const [isOpen, setIsOpen] = React.useState(false);
    return (
      <>
        <Button variant="primary" onClick={() => setIsOpen(true)}>
          Open
        </Button>
        <Modal
          isOpen={isOpen}
          style={{ width: 500, height: 200 }}
          onClose={() => setIsOpen(false)}
        >
          <Button onClick={() => setIsOpen(false)}>‍Close</Button>
        </Modal>
      </>
    );
  }}
</Playground>

## Multiple modals

<Playground>
  {() => {
    const [isOpen, setIsOpen] = React.useState(false);
    const [isAnotherOpen, setIsAnotherOpen] = React.useState(false);
    const [isYetAnotherOpen, setIsYetAnotherOpen] = React.useState(false);
    return (
      <>
        <Button variant="primary" onClick={() => setIsOpen(true)}>
          Open
        </Button>
        <Modal
          isOpen={isOpen}
          style={{ width: 500, height: 200 }}
          onClose={() => setIsOpen(false)}
        >
          <Button onClick={() => setIsOpen(false)}>‍Close</Button>
          <Button variant="primary" onClick={() => setIsAnotherOpen(true)}>
            Open another
          </Button>
        </Modal>
        <Modal
          isOpen={isAnotherOpen}
          style={{ width: 300, height: 100 }}
          onClose={() => setIsAnotherOpen(false)}
        >
          <Button onClick={() => setIsAnotherOpen(false)}>‍Close</Button>
          <Button variant="primary" onClick={() => setIsYetAnotherOpen(true)}>
            Open another
          </Button>
        </Modal>
        <Modal
          isOpen={isYetAnotherOpen}
          style={{ width: 200, height: 50 }}
          onClose={() => setIsYetAnotherOpen(false)}
        >
          <Button onClick={() => setIsYetAnotherOpen(false)}>‍Close</Button>
        </Modal>
      </>
    );
  }}
</Playground>

## Properties

<Props of={Modal} />
