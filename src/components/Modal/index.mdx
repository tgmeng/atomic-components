---
name: Modal
menu: Components
---

import { Playground, Props } from 'docz';
import Button from '../Button';
import Modal from './index.tsx';

# Modal

<Playground>
  {() => {
    const [isOpen, setIsOpen] = React.useState(false);
    return (
      <>
        <Button variant="primary" onClick={() => setIsOpen(true)}>
          Open
        </Button>
        <Modal isOpen={isOpen} style={{ width: 500, height: 200 }}>
          <Button onClick={() => setIsOpen(false)}>‍Close</Button>
        </Modal>
      </>
    );
  }}
</Playground>

# Multiple modals

<Playground>
  {() => {
    const [isOpen, setIsOpen] = React.useState(false);
    const [isAnotherVisible, setIsAnotherVisible] = React.useState(false);
    const [isYetAnotherVisible, setIsYetAnotherVisible] = React.useState(false);
    return (
      <>
        <Button variant="primary" onClick={() => setIsOpen(true)}>
          Open
        </Button>
        <Modal isOpen={isOpen} style={{ width: 500, height: 200 }}>
          <Button onClick={() => setIsOpen(false)}>‍Close</Button>
          <Button variant="primary" onClick={() => setIsAnotherVisible(true)}>
            Open another
          </Button>
        </Modal>
        <Modal isOpen={isAnotherVisible} style={{ width: 300, height: 100 }}>
          <Button onClick={() => setIsAnotherVisible(false)}>‍Close</Button>
          <Button variant="primary" onClick={() => setIsYetAnotherVisible(true)}>
            Open another
          </Button>
        </Modal>
        <Modal isOpen={isYetAnotherVisible} style={{ width: 200, height: 50 }}>
          <Button onClick={() => setIsYetAnotherVisible(false)}>‍Close</Button>
        </Modal>
      </>
    );
  }}
</Playground>

## Properties

<Props of={Modal} />
