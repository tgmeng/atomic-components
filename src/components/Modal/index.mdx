---
name: Modal
menu: Components
---

import { Playground, Props } from 'docz';
import Button from '../Button';
import Modal from './index.tsx';

# Modal

<Playground>
  {() => {
    const [isVisible, setIsVisible] = React.useState(false);
    return (
      <>
        <Button variant="primary" onClick={() => setIsVisible(true)}>
          Open
        </Button>
        <Modal isVisible={isVisible} style={{ width: 500, height: 200 }}>
          <Button onClick={() => setIsVisible(false)}>‍Close</Button>
        </Modal>
      </>
    );
  }}
</Playground>

# Multiple modals

<Playground>
  {() => {
    const [isVisible, setIsVisible] = React.useState(false);
    const [isAnotherVisible, setIsAnotherVisible] = React.useState(false);
    return (
      <>
        <Button variant="primary" onClick={() => setIsVisible(true)}>
          Open
        </Button>
        <Modal isVisible={isVisible} style={{ width: 500, height: 200 }}>
          <Button onClick={() => setIsVisible(false)}>‍Close</Button>
          <Button variant="primary" onClick={() => setIsAnotherVisible(true)}>
            Open another
          </Button>
        </Modal>
        <Modal isVisible={isAnotherVisible} style={{ width: 300, height: 100 }}>
          <Button onClick={() => setIsAnotherVisible(false)}>‍Close</Button>
        </Modal>
      </>
    );
  }}
</Playground>

## Properties

<Props of={Modal} />
