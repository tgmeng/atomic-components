---
name: Input
menu: Components
---

import { Playground, Props } from 'docz';

import { Alert } from '../Alert';
import { Link } from '../Link';

import { Input } from './';

# Input

## 不同大小

<Playground>
  <Input size="small" defaultValue="Small" />
  <Input size="normal" defaultValue="Normal" />
  <Input size="large" defaultValue="Large" />
</Playground>

## 前缀和后缀

<Playground>
  <Input prefix={<span>Prefix</span>} />
  <Input suffix={<span>Suffix</span>} />
</Playground>

## 禁用

<Playground>
  <Input defaultValue="Disabled" disabled />
</Playground>

## 可清空

<Playground>
  {() => {
    const [value, setValue] = React.useState('🌚 👉');
    return (
      <Input
        value={value}
        isClearable
        onChange={(e) => setValue(e.target.value)}
      />
    );
  }}
</Playground>

<Alert
  style={{
    marginTop: 15,
  }}
  intent="warning"
>
  <>
    如果 Input 是{' '}
    <Link
      href="https://reactjs.org/docs/uncontrolled-components.html"
      target="_blank"
    >
      <i>uncontrolled component</i>
    </Link>
    , <i>isClearable</i> 将不起作用. 请将 Input 改为 <Link
      href="https://reactjs.org/docs/forms.html#controlled-components"
      target="_blank"
    >
      <i>controlled component</i>
    </Link>.
  </>
</Alert>

## 属性

<Props of={Input} />
